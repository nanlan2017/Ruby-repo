=begin
1. Array 是动态的。即使array.size=8， 但 array[20]=3 会自动扩展array 的长度、并插入数据
2. Array 可以同时容纳不同类型的elements .

3.  ████ map , fold , filter
array.each do |pattern|
    /... 每个元素的处理
end
——————  这相当于 Haskell 中对 List 进行map

map (\pattern-> ...) [...]


=end
